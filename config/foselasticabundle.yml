fos_elastica:
   clients:
       default:
           host: %fos_elastica.host%
           port: %fos_elastica.port%
   indexes:
       search:
           finder: ~
           client: default
           types:
               books:
                   mappings:
                       id:
                           type: integer
                       title: ~
                       author: ~
                   persistence:
                       driver: orm
                       model: AppBundle\Entity\Prestataires
                       finder: ~
                       provider: ~
                       listener: ~
{% extends 'base.html.twig' %}

{% block title %}Profil | {{ user.username }}{% endblock %}

{% block body %}
    <h3 class='text-center text-info p-2 bg-white '>{{ user.username }}</h3>
    <div class='text-dark bg-white text-center d-flex p-1'>   
        <a class=' mr-2 btn btn-info disabled' href="{{ path('profil_edit', {'id': user.id}) }}">Modifier</a>
        {{ include('profil/_delete_form.html.twig') }}
    </div>
    <div class='row'>
        <div class='col-4'>
            <img class='bg-white p-1' src='{{ user.img }}'></img>
        </div>    
        <div class='col-8'>
            <table class="table text-white">
        <tbody>
            <tr>
                <th>ID</th>
                <td><code>{{ user.id }}<code></td>
            </tr>
            <tr>
                <th>Nom de l'utilisateur</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Url image de profil</th>
                <td><b><code> {{ user.img }} </code></b></td>
            </tr>
>
            <tr>
                <th>N°Rue et rue</th>
                <td>
                    <adresse>
                        {{ user.adresseNum }}, {{ user.adresse }} <br>
                    </address>
                </td>
            </tr>
        </tbody>
    </table>

        </div>
    </div>
    <a class='btn btn-lg text-white' href="{{ path('profil_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a>
{% endblock %}

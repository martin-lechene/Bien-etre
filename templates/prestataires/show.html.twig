{% extends 'base.html.twig' %}

{% block title %}Prestataires{% endblock %}

{% block body %}
    <h1>Prestataires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prestataire.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prestataire.name }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ prestataire.website }}</td>
            </tr>
        </tbody>
    </table>

    <a class='btn btn-outline-warning' href="{{ path('prestataires_index') }}">back to list</a>

    <a class='btn btn-outline-info' href="{{ path('prestataires_edit', {'id': prestataire.id}) }}">edit</a>

    {{ include('prestataires/_delete_form.html.twig') }}
{% endblock %}

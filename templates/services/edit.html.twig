{% extends 'base.html.twig' %}

{% block title %}Edition d'un service{% endblock %}

{% block body %}
    <h3 class='text-center text-info bg-white p-2'>{{service.name}}</h3>
    {{ include('services/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <a class='btn btn-lg text-white' href="{{ path('services_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a>
    {{ include('services/_delete_form.html.twig') }}
{% endblock %}
